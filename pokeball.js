const { Pokemon, Fire, Water, Normal, Grass, Charmander, Squirtle, Bulbasaur, Rattata } = require("../fun-pokemon-battler/index")

const Pokeball = []
if(Pokeball.length === 0) {
    Pokeball.push(Pokemon)
    return Pokeball
}

module.exports = { Pokeball }